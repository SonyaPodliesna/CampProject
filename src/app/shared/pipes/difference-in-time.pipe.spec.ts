import { DifferenceInTimePipe } from './difference-in-time.pipe';

describe('DifferenceInTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DifferenceInTimePipe();
    expect(pipe).toBeTruthy();
  });
});
